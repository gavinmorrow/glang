use crate::ast::{
    BinaryExpr, Binding, Block, Call, Expr, Identifier, IfExpr, Literal, Program, Stmt, UnaryExpr,
};

pub fn gen_wasm(program: Program) -> () {
    let mut out = todo!();

    for stmt in program {
        todo!()
    }

    out
}
